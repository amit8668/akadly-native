export const esp32 = {
  id: 'esp32',
  label: 'ESP32',
  // Category ids this board's toolbox includes, in display order.
  // Stage 1 only ships 'gpio' and 'timing' - more are added as the block
  // library grows (see the build plan).
  categories: ['gpio', 'timing'],
};
